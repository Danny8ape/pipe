import { StringMayusInterPipe } from './string-mayus-inter.pipe';

describe('StringMayusInterPipe', () => {
  it('create an instance', () => {
    const pipe = new StringMayusInterPipe();
    expect(pipe).toBeTruthy();
  });
});
