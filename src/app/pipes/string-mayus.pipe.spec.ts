import { StringMayusPipe } from './string-mayus.pipe';

describe('StringMayusPipe', () => {
  it('create an instance', () => {
    const pipe = new StringMayusPipe();
    expect(pipe).toBeTruthy();
  });
});
