import { ImplementsPipe } from './implements.pipe';

describe('ImplementsPipe', () => {
  it('create an instance', () => {
    const pipe = new ImplementsPipe();
    expect(pipe).toBeTruthy();
  });
});
